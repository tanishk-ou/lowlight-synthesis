# Auto-detect text files and normalise line endings
* text=auto

# Source code — enforce LF
*.py text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.toml text eol=lf
*.json text eol=lf

# Data files (binary, no diff)
*.json diff
