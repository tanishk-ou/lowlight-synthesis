"""Setup configuration for lowlight-synthesis package."""
from setuptools import setup, find_packages

setup(
    name="lowlight-synthesis",
    version="1.0.0",
    packages=find_packages(),
    python_requires=">=3.8",
)
